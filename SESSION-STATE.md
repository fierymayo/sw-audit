# SESSION-STATE — sw-audit
Updated: 2026-09-21 (evidence/buckets complete)

## Where things stand
- All features delivered and committed: audits, collections engine + Adds + XLSX,
  entity-evidence (24 rows, gated), surname-buckets, run folders, UTC+5 stamps,
  run_all.py, UI buttons, HOW-IT-WORKS.md. 55 tests green.
- Entity evidence final: 5 product-backed rows + 14 unrostered clubs + 3 local
  entities + 1 typo'd collection (Villareal, one "r") + 1 thematic residue.
- Surname buckets delivered the Martinez-class split (club context + first names).

## Pending / parked
1. Merchant questions: "No Club" ×20 sentinel; 4 ZERO_WITH_RULE dead collections;
   Villareal collection-title typo; colour rules (Red rejected: 206/360 false matches).
2. Design questions (frozen with Mechanic edits): surname qualified-keywords vs
   star-only — surname-buckets CSV is the dataset; roster expansion list = entity
   evidence rows 6–24.
3. First applied forecast should be verified against actual task results (parity §9).

## Backlog (unscoped)
- Dashboard artifact for run folders.